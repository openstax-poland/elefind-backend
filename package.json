{
  "name": "elefind-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/*.test.js --timeout 5000",
    "test-watch": "nodemon --exec \"npm test\""
  },
  "engines": {
    "node": "9.5.0"
  },
  "author": "Piotr Kozłowski for OpenStax Poland",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "express-rate-limit": "^3.3.2",
    "jsdom": "^13.0.0",
    "puppeteer": "^1.10.0"
  },
  "devDependencies": {
    "expect": "^22.4.0",
    "mocha": "^5.0.1",
    "nodemon": "^1.18.4",
    "supertest": "^3.0.0"
  }
}
